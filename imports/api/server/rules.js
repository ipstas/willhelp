/* import { Collections } from '../collections.js';
Collections.Texts.allow({
  update: function() {
		console.log('[Collections.Texts.allow rules] insert', Roles.userIsInRole(userId, ['admin'], 'admGroup'), this.userId, userId );
    return true;
  }
}); */